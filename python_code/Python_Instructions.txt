#!/usr/bin/python
# -*- coding: utf-8 -*-

"""
---------------------------------------------------------------------------------------------

python中的对象包含3要素：
id ：            用来唯一标识一个对象
type:            标识对象类型
value：          对象的值

---------------------------------------------------------------------------------------------

is :             判断两个对象是否相等，通过id判断
== :             判断两个对象的值是否相等，通过value判断

---------------------------------------------------------------------------------------------

d = {'a':'a', 'b':'b'}
dd = d.copy()
dd
id(d)
id(dd)
dd is d
dd == d

---------------------------------------------------------------------------------------------

#   合并字符串

#   当list非常大的时候，会消耗很大的内存，建议用join

colors = ['red', 'blue', 'green', 'yellow']
result = ''
for s in colors:
    result += s

#   join只能用于元素是字符串的list，它不会进行任何的强制类型转换。连接一个存在一个或多个非字符串元素的list时将抛出异常

result = ','.join(colors)


---------------------------------------------------------------------------------------------
"""
